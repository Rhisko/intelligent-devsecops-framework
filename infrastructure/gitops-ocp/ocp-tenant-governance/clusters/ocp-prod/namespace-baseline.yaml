apiVersion: platform.company.io/v1alpha1
kind: ClusterEntrypoint
metadata:
  name: namespace-baseline
spec:
  cluster: ocp-prod
  path: infrastructure/gitops-ocp/ocp-tenant-governance/platform/namespaces/prod
  managedResources:
    - Namespace
    - ResourceQuota
    - LimitRange
    - NetworkPolicy

