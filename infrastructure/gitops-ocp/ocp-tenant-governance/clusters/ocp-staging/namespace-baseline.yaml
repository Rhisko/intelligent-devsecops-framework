apiVersion: platform.company.io/v1alpha1
kind: ClusterEntrypoint
metadata:
  name: namespace-baseline
spec:
  cluster: ocp-staging
  path: infrastructure/gitops-ocp/ocp-tenant-governance/platform/namespaces/staging
  managedResources:
    - Namespace
    - ResourceQuota
    - LimitRange
    - NetworkPolicy

